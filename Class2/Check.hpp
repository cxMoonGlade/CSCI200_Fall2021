#ifndef CIRCLE_H
#define CIRCLE_H
// @avai for availability for this method. 
// This method is using the Gauss Algorithm to calculation the sum
int gauss(char avai);

// here is how we write a program when we cannot find out a correct algorithm, this takes index level more memories costing, energy costing, and so on. 
//When the case is bad enough, you may never get an answer until you have a better computer or a better algorithm.
// we just enter the whole array into it.
// I am not using linked list here, cause that might be slower than using array.
int just_do_it();
#endif